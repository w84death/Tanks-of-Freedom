extends "res://scripts/unit_control.gd"


func can_capture_empty_building(unit):
	print('checking if can capture empty building')

func can_capture_enemy_building(unit):
	print('checking if can capture enemy building')

func can_attack_unit(unit):
	print('checking if can attak unit')


